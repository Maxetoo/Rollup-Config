{
    "name": "bundler",
    "version": "1.0.0",
    "description": "Rollup Package",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "rollup -c --watch",
        "build": "rollup -c",
        "dev": "rollup src/main.js -f cjs -d dist"
    },
    "author": "Maxy",
    "license": "MIT",
    "devDependencies": {
        "rollup": "^2.75.6"
    },
    "dependencies": {
        "rollup-plugin-css-only": "^3.1.0"
    }
}
